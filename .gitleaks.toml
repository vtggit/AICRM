# Gitleaks configuration for AICRM
#
# Lightweight secret-pattern scanning with minimal exclusions.
# The .env template holds placeholder values that are intentionally public;
# real documentation (README.md, backend/README.md) is secret-scanned, not exempt.

[allow]
  paths = [
    # Environment template — placeholder values only
    '''.env\.example''',
  ]
